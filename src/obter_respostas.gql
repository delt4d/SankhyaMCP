query replies($after: String, $before: String, $excludePins: Boolean, $limit: Int!, $offset: Int, $orderBy: PostListOrderByEnum, $postId: ID!, $reverse: Boolean) {
  replies(
    after: $after
    before: $before
    excludePins: $excludePins
    limit: $limit
    offset: $offset
    orderBy: $orderBy
    postId: $postId
    reverse: $reverse
  ) {
    totalCount
    nodes {
        id
        mappingFields {
            key
            type
            value
        }
        owner {
            member {
                name
            }
        }
        repliedToId
        replies(limit: 100, reverse: true, orderBy: createdAt) {
            nodes {
                id
                owner {
                    member {
                        name
                    }
                }
                shortContent
            }
            pageInfo {
                endCursor
                hasNextPage
            }
            totalCount
        }
        publishedAt
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}